export interface PostModel {
     id:string;
    title:string;
     content:string;
     description: string;
     imagePath : string;
     creator : string;
}
     /*
//PostModel is interface model which act are type checker to all file if any
 type is not mached the error is through
*/
